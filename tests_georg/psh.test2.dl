% produce and redirect
storage_plant(pp). c_max(pp, 15). w_charge(10).
c(pp, i1, 9). % from control
p(pp, i2, 3). % from switch
c(pp, is, adsf). % storage

expect_c(pp, i1, 9).
expect_p(pp, i2, 3).
expect_c(pp, is, adsf).
expect_p(pp, op, 12). 
