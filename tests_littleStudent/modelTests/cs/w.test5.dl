%Author: Simerle Christopher

%Testfile 1 for windmill
%normal output (max. capacity)

windmill(w). 
c_max(w, 40).
c(w, i1, 60). 
c(w, iw, 30).
expect_c(w, i1, 60).
expect_c(w, iw, 30).
expect_p(w, op, 40).