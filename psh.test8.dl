storage_plant(psh).

c_max(psh, 20). 
w_charge(15).

c(psh, i1, charge).
c(psh, is, full).
p(psh, i2, 10).

expect_c(psh, i1, charge).
expect_c(psh, is, full).
expect_p(psh, i2, 10).

expect_p(psh, op, 10).
